
<!--
<div class="card" style="width: 18rem;">
  <img class="card-img-top" src="/img/camisa.jpeg" alt="Imagem de capa do card">
</div>
-->

{{#each erros}}
<div class="alert alert-danger"> {{texto}}</div>
{{else}}

{{/each}}

<div class="card h-50 d-inline-block">
    <div class="card-header">
        <h5>Dados Pedido: <a href='/pedidos'> <button class='btn btn-info'>Listar Camisas</button></a>
            <a href="/"> <button class="btn btn-dark">Inicio</button> </a> 
        </h5>
    </div>
    <div class="card-body">
        <form action="/pedidos/nova" method="POST">
            <div class="form-row">
                <div class="col">
                    <label for="nome">Nome:</label>
                    <input type="text" name="nome" class="form-control " >
                </div>

                <div class="col">
                    <div class="form-group">
                        <label for="tamanho">Tamanho:</label>
                        <select name="tamanho" id="tamanho" class="form-control">
                            <option selected>P</option>
                            <option>M</option>
                            <option>G</option>
                            <option>GG</option>
                            <option>XGG</option>
                            <option>------</option>
                            <option>B. Look P</option>
                            <option>B. Look M</option>
                            <option>B. Look G</option>
                            <option>B. Look GG</option>

                        </select>
                    </div>
                </div>

                <div class="col">
                    <label for="familia">Familia</label>
                    <select name="familia" class="custom-select">
                        {{#each familias}}
                        <option value="{{_id}}">{{nome}}</option>
                        {{else}}
                        <option value="0">Nenhuma Familia registrada</option>
                        {{/each}}
                    </select>

                </div>

            </div>
            <button class="btn btn-success mt-4" type="submit">Gravar Pedido</button>
        </form>
    </div>
  <footer class="blockquote-footer">Informe o nome de cada pessoa da familia <cite title="Título da fonte"></cite></footer> </a>

</div>